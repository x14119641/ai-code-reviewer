{
  "name": "Replacing join with repeated string concatenation introduces loop allocation",
  "before_path": "before.py",
  "after_path": "after.py",
  "expected_issues": [
    {
      "severity": "low",
      "rule": "string_concatenation_in_loop",
      "category": "performance",
      "explanation": "The diff replaces list accumulation and join with repeated string concatenation inside the loop, causing repeated string allocations as the output grows."
    }
  ]
}